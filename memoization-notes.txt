Memoization

- Stores the arguments of each function call along with the result
- If the function gets called with the same args, return the precomputed result
- So you don't have to run the function again
